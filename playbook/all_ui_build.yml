- name: Build for RBMR
  hosts: '{{ server }}'
  remote_user: "{{ build_user }}"
  tags:
    - RBMR
  roles:
    - clean_workarea_rv
    - build_cout_rv
    - build_run
    - setup_exe_rv
    - final_archive
    - copy_zip_rb
- name: Build for RBFULL
  hosts: '{{ server }}'
  remote_user: "{{ build_user }}"
  tags:
    - RBFULL
  roles:
    - clean_workarea_rv
    - build_cout_rv
    - build_run
    - setup_exe_rv_patch
    - final_archive
    - copy_zip_rb
- name: Build for RBDELTA
  hosts: '{{ server }}'
  remote_user: "{{ build_user }}"
  tags:
    - RBDELTA
  roles:
    - clean_workarea_rv
    - build_cout_rv
    - build_run
    - copy_del
- name: Build for TAP
  hosts: '{{ server }}'
  remote_user: "{{ build_user }}"
  tags:
    - TAP
  roles:
    - clean_workarea_tap
    - build_cout_tap
    - build_run
    - setup_exe_tap
    - final_archive
- name: Build for VI
  hosts: '{{ server }}'
  remote_user: "{{ build_user }}"
  tags:
    - VI
  roles:
    - clean_workarea_rv
    - build_cout_rv
    - setup_exe_rv
    - final_archive
    

  
